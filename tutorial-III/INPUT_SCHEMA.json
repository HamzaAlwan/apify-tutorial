{
  "title": "Filter by cheapest offer",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "datasetId": {
      "title": "Dataset ID",
      "type": "string",
      "description": "The id of the dataset we want to filter",
      "editor": "textfield"
    }
  },
  "required": ["datasetId"]
}
